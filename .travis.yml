language: csharp
os: linux
mono: none
dotnet: 3.1
dist: xenial
script:
  - dotnet restore
  - dotnet build
  - dotnet test ./TravisTest.Tests -c Release -f 3.1
after_script:
  - bash <(curl -s https://codecov.io/bash)
